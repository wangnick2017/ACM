<Window x:Class="ACM.Dialogs.DgReg"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ACM"
        mc:Ignorable="d"
        Title="{DynamicResource dgreg.title}" Height="350" Width="300" Style="{StaticResource ACMDialogStyle}" WindowStartupLocation="CenterScreen" Loaded="Window_Loaded" ResizeMode="NoResize">
    <Grid>
        <TextBlock x:Name="txt" TextWrapping="Wrap" Margin="20,20,20,70" FontSize="18" FontWeight="Light">
        </TextBlock>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,15" Orientation="Horizontal">
            <Button Style="{StaticResource PageButtonStyle}" Content="{DynamicResource dgreg.login}" IsDefault="True" Margin="5" Padding="30,10" Click="Button1_Click"/>
            <Button Style="{StaticResource PageButtonStyle}" Content="{DynamicResource return}" Margin="5" Padding="30,10" Click="Button2_Click"/>

        </StackPanel>

    </Grid>
</Window>
